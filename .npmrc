# mind-ar pulls in the native `canvas` module for its Node-side compiler, which
# we never use (we only ship the browser bundles). Skip native build scripts so
# installs work without Python/MSVC (local) or system cairo libs (Vercel Linux).
ignore-scripts=true
